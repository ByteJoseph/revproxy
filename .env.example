FLASK_SECRET_KEY=replace-with-a-long-random-secret
# Preferred: use hashed password
DASHBOARD_PASSWORD_HASH=replace-with-generated-hash

# Optional fallback if you do not want hash (not recommended)
# DASHBOARD_PASSWORD=replace-with-plain-password
